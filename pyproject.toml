[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "nydra"
version = "0.4.0"
description = "Autonomous Data Inspection Agent"
readme = "README.md"
authors = [
    { name = "Nydra Contributors" }
]
license = { text = "MIT" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Rust",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.11"
dependencies = [
    "plotly>=5.18.0",
    "streamlit>=1.30.0",
    "pandas>=2.0.0",
    "numpy>=1.26.0",
    "openpyxl>=3.1.0",
    "python-dotenv>=1.0.0",
    "pytest>=7.4.0",
    "jsonschema>=4.20.0",
    "scikit-learn>=1.3.0",
    "scipy>=1.11.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "python-multipart>=0.0.6",
    "supabase>=2.0.0",
    "joblib>=1.3.0",
    "shap>=0.42.0",
    "torch",
    "maturin>=1.5",
]

[tool.maturin]
manifest-path = "nydra-core/Cargo.toml"
python-source = "src"
module-name = "nydra_core"

[project.scripts]
nydra = "cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "cli.py"]
