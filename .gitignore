# ── Frontend (Node/Next.js) ──
frontend/node_modules/
frontend/.next/
frontend/.env.local
frontend/tsconfig.tsbuildinfo
frontend/package-lock.json

# ── Databases ──
*.db
*.db-shm
*.db-wal
*.sqlt

# ── Secrets / certs ──
*.pem
.env
.env.*

# ── User uploads (runtime data, not source) ──
uploads/

# ── Generated reports (runtime output, not source) ──
reports/

# ── Debug / recovery artifacts from this session ──
api_BUFFER_BACKUP.py
api_OLD_aug20.py
api.py.diskversion_aug20
api_diff.txt
debug_test.db
*_output.txt
git_status_full.txt

# ── Large binary datasets (don't bloat repo history) ──
examples/
mnist-dataset.zip

# ── IDE (in case something slips through despite .idea/ above) ──
.idea/
# Virtual environment
.venv/

# VS Code
.vscode/

# Python bytecode
__pycache__/
*.pyc
